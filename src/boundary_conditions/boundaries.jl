function fix_boundaries!(grid::Grid)
    # Nx, Ny = size(grid.pos)[2], size(grid.pos)[3]
    # for i in 1:Nx
    #     grid.v_new[:, i, 1] .= 0.0
    #     grid.v_new[:, i, Ny] .= 0.0
    #     grid.v[:, i, 1] .= 0.0
    #     grid.v[:, i, Ny] .= 0.0
    #     grid.momentum_new[:, i, 1] .= 0.0
    #     grid.momentum_new[:, i, Ny] .= 0.0
    #     grid.momentum[:, i, 1] .= 0.0
    #     grid.momentum[:, i, Ny] .= 0.0

    # end
    return nothing
end